-- auto-generated definition
create function show_subscription()
    returns TABLE(name character varying, detail character varying, rank character varying, price numeric)
    language plpgsql
as
$$
begin
    -- missing source code
end;
$$;

alter function show_subscription() owner to sc_db_project_user;

